// import { mount } from '@vue/test-utils'
import { describe, expect, test } from 'vitest'
// import FocusTrap from '../index'

describe('FocusTrap', () => {
  test('class', () => {
    // const wrapper = mount(FocusTrap)
    // expect(wrapper.classes()).toContain('fz-focus-trap')
    expect(true)
  })
})
