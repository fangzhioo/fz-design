// import { mount } from '@vue/test-utils'
import { describe, expect, test } from 'vitest'
// import Popper from '../index'

describe('Popper', () => {
  test('class', () => {
    // const wrapper = mount(Popper)
    // expect(wrapper.classes()).toContain('fz-popper')
    expect(true)
  })
})
